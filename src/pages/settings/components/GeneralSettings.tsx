import { Note } from '../../../common/types';

type Props = {
    notes: Note[];
};

const GeneralSettings = (props: Props) => {
    return <div className="settings-tab">GeneralSettings</div>;
};

export default GeneralSettings;
